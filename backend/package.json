{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/venv-run.js python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
    "build": "echo 'No build for backend'",
    "lint": "node scripts/venv-run.js ruff check . && node scripts/venv-run.js black --check .",
    "format": "node scripts/venv-run.js ruff check --fix . && node scripts/venv-run.js black ."
  }
}
